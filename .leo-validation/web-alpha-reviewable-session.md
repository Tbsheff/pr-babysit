# Web alpha reviewable session fixture

This deterministic validation file is maintained by `bun run db:seed`.
It gives the seeded web-alpha reviewable session a session-scoped changed file for GitHub PR delivery.
